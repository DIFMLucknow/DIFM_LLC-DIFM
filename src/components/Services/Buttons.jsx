import React from "react";

const Buttons = ({ styles }) => (
  <button type="button" className={`py-4 px-6 font-poppins font-medium text-[16px] text-primary bg-blue-gradient rounded-[10px] outline-none ${styles}`}>
    Contact Now
  </button>
);

export default Buttons;
